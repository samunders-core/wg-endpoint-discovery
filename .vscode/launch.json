{
    // Use IntelliSense to learn about possible attributes.
    // Hover to view descriptions of existing attributes.
    // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "F5 Anything: My Custom Command",
            "type": "f5anything",
            "request": "launch",
            "command": "bash -c '(cd ${fileDirname} && ./roundup ${fileBasename})'",
            //"command": "bash -c 'zip redbean.com .init.lua .lua/*.lua && cp redbean.com ${fileDirname} && (cd ${fileDirname} && ./roundup ${fileBasename})'",
        }
    ]
}